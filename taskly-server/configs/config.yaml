server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: ""
  name: taskly_dev
  sslmode: disable

jwt:
  secret: "change-me-in-production"
  expire_hours: 720  # 30 days

stripe:
  secret_key: "sk_test_..."
  publishable_key: "pk_test_..."
  webhook_secret: "whsec_..."

apple:
  team_id: "K65RWCXUXM"
  bundle_id: "com.taskly.app"

upload:
  max_size_mb: 10
  allowed_types:
    - image/jpeg
    - image/png
    - image/webp

commission:
  rate: 0.0   # 0% early stage; change to 0.10 when ready
